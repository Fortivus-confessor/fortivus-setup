# RabbitMQ — Configuração (HOM/Produção)
# Carrega a topologia de exchanges, queues e bindings no boot.
# Os usuários vêm das variáveis de ambiente RABBITMQ_DEFAULT_USER/PASS.
load_definitions = /etc/rabbitmq/definitions.json
